#' geom_pub
#'
#' geom values to be used with published figures
#' 
#' @export

geom_pub <- list("point" = list(size = 0.75),
                "line" = list(size = 0.2),
                "errorbar" = list(size = 0.2))
